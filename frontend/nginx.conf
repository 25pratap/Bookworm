server {
    listen 80;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    # React Router routes (such as /admin/login) are handled client-side.
    location / {
        try_files $uri $uri/ /index.html;
    }
}
